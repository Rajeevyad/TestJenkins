pipeline {
    agent any 
    stages {
        stage('Running PowerShell script............') {
            steps {
                  echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
                  powershell '.\\build.ps1'
            }
        }
    }
}